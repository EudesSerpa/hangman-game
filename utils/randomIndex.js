/**
 * It returns a random index of an array
 * @param array - the array to choose a random index from
 * @returns {number} - the random index of the array
 */
export const randomIndex = (array) => Math.floor(Math.random() * array.length);
